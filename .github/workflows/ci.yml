name: CI Quality Gates

on:
  pull_request:
    branches: [develop, main]
  push:
    branches: [develop]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.CI_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.CI_SUPABASE_ANON_KEY }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.CI_SUPABASE_SERVICE_ROLE_KEY }}
      NEXT_PUBLIC_GOOGLE_MAPS_API_KEY: ${{ secrets.CI_GOOGLEMAP_API_KEY }}
      NEXT_PUBLIC_APP_URL: ${{ secrets.CI_APP_URL }}
      CI: "true"
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck shared packages
        run: |
          pnpm --filter @nexvoy/types typecheck
          pnpm --filter @nexvoy/design-tokens typecheck
          pnpm --filter @nexvoy/core typecheck

      - name: Build web app
        run: pnpm --filter nexvoy-web build

      - name: Verify mobile app
        run: |
          pnpm --filter nexvoy-app lint
          pnpm --filter nexvoy-app typecheck
          pnpm --filter nexvoy-app build
